name: Build Alex

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        shell: cmd
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller

      - name: Build Alex Debug
        shell: cmd
        run: |
          python -m PyInstaller --name Alex --onefile main.py

      - name: Verify EXE
        shell: cmd
        run: |
          if not exist "dist\Alex.exe" (
            echo Alex.exe was not created.
            exit /b 1
          )
          echo Alex.exe created successfully.
          dir "dist\Alex.exe"

      - name: Upload Alex
        uses: actions/upload-artifact@v4
        with:
          name: Alex-Windows-Debug
          path: dist/Alex.exe
          if-no-files-found: error
